#pragma once
#include "uri.h"

namespace ara
{
    namespace rest
    {
        class Resolve
        {
        private:
        public:
            Resolve();
            ~Resolve();
        };

        Resolve::Resolve() {}
        Resolve::~Resolve() {}
    }
}
