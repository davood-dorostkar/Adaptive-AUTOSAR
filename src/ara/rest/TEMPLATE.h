#pragma once

namespace ara
{
    namespace rest
    {
        class Allocator
        {
        private:
        public:
            Allocator();
            ~Allocator();
        };

        Allocator::Allocator() {}
        Allocator::~Allocator() {}
    }
}
